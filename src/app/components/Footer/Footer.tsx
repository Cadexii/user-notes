import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <div className={styles.footerContainer}>
      <h1 className={styles.footerText}>UserNotes</h1>
    </div>
  );
};

export default Footer;
